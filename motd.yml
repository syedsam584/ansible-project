---
- name: playbook massage of the day
  hosts: web 
  gather_facts: false
  tasks: 
   - name: copy to massage of the day 
     copy:
      src: /root/hosts_motd
      dest: /etc/motd
     remote_src: yes 

              
